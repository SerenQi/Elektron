# 记忆/状态数据目录
ELEKTRON_HOME=./data
# agent 进程目录（放 PID 文件，行为层唤醒用）
AGENT_HOME=./agent
# 自动打标用的小模型（不填则降级为默认值，系统仍可运行）
DEHYDRATOR_API_KEY=
DEHYDRATOR_BASE_URL=https://api.deepseek.com/v1
DEHYDRATOR_MODEL=deepseek-chat
